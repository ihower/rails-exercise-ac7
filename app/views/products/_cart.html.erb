<ul>
<% current_cart.line_items.each do |line| %>
  <li>
    <%= line.product.name %> x <%= line.qty %> = <%= line.amount %>
    <%= link_to "[x]", cancel_product_path(line.product), :method => :post, :remote => true %>
  </li>
<% end %>
</ul>

<% current_cart %>
<% current_cart %>
<% current_cart %>
